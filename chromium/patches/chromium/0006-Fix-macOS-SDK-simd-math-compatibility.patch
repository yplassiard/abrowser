diff --git a/components/metal_util/hdr_copier_layer.mm b/components/metal_util/hdr_copier_layer.mm
index 9e6681577b..04908a6fb3 100644
--- a/components/metal_util/hdr_copier_layer.mm
+++ b/components/metal_util/hdr_copier_layer.mm
@@ -6,6 +6,16 @@
 
 #include <CoreVideo/CVPixelBuffer.h>
 #include <Metal/Metal.h>
+
+// Workaround for newer macOS SDKs missing __sincospif/__sincospi
+#include <math.h>
+#ifndef __sincospif
+#define __sincospif(x, sinp, cosp) __sincosf((x) * M_PI, sinp, cosp)
+#endif
+#ifndef __sincospi
+#define __sincospi(x, sinp, cosp) __sincos((x) * M_PI, sinp, cosp)
+#endif
+
 #include <MetalKit/MetalKit.h>
 
 #include "base/mac/foundation_util.h"
diff --git a/ui/gfx/hdr_metadata_mac.mm b/ui/gfx/hdr_metadata_mac.mm
index b3ed229c76..ab5a0f2375 100644
--- a/ui/gfx/hdr_metadata_mac.mm
+++ b/ui/gfx/hdr_metadata_mac.mm
@@ -5,6 +5,15 @@
 #include "ui/gfx/hdr_metadata_mac.h"
 #include "ui/gfx/hdr_metadata.h"
 
+// Workaround for newer macOS SDKs missing __sincospif/__sincospi
+#include <math.h>
+#ifndef __sincospif
+#define __sincospif(x, sinp, cosp) __sincosf((x) * M_PI, sinp, cosp)
+#endif
+#ifndef __sincospi
+#define __sincospi(x, sinp, cosp) __sincos((x) * M_PI, sinp, cosp)
+#endif
+
 #include <simd/simd.h>
 
 namespace gfx {
